# hello

Minimal example image used as the working sample for this monorepo.

## Build

```sh
make build IMAGE=hello
```

## Run

```sh
docker run --rm ghcr.io/breauxaj/docker-images/hello:latest
```
